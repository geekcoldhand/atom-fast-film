# overview

A mobile-first web app that applies a real-time cyanotype/analog film aesthetic to photos. The user uploads an image,
adjusts visual parameters via sliders, and saves the processed result. I want to make a film filter over digital photos.
The input is a raw image and should save this image to photos as output.
